# zkSYS PoB Devnet (57042) — clave privada de la wallet que despliega y paga gas
# Usa la misma wallet registrada en el programa (100 TSYS acreditados)
PRIVATE_KEY=

# Direcciones de contratos (usadas por el backend)
SONETYO_NFT_ADDRESS=
PROJECT_VAULT_ADDRESS=

# Pinata (IPFS) — opcional
PINATA_API_KEY=
PINATA_SECRET=

# RPC y base de datos
RPC_URL=https://rpc-pob.dev11.top
DB_CLIENT=sqlite3
DATABASE_URL=./dev.db
